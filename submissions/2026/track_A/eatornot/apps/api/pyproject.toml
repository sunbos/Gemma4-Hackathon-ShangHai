[project]
name = "eatornot-api"
version = "0.1.0"
description = "EatOrNot API — Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "httpx",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
